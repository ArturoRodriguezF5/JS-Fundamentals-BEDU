const auto = {
    marca: "Honda",
    modelo: "Civic",
    color: "Negro",
    anio: 2024
};

auto.anio = 2025;
console.log(auto);
console.log(auto.modelo);

const keys = Object.keys(auto);

console.log(keys);

const pairs = [] 

for (let i = 0; i < keys.length; i++) {
    pairs
}

